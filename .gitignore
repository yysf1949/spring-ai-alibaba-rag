# Build artifacts
target/
build/
out/
*.class
*.jar
*.war
*.ear

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.project
.classpath
.settings/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Local env / secrets — NEVER commit
.env
.env.local
application-local.yml
application-local.yaml
application-dev.yml
**/secrets.yml
**/secrets.yaml
**/secrets.env
# rag-embedding env templates: keep the .example, ignore the real .env
rag-embedding/.env
# User runtime secrets (e.g. SiliconFlow API key) — never commit
.rag-runtime/
# Local Maven staging for docker builds (see scripts/build-docker.sh)
.docker-maven/
# Hermes-managed secrets store
.hermes/secrets/

# Maven wrapper (we use system maven 3.9.16)
.mvn/
mvnw
mvnw.cmd