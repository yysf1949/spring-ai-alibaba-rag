-- MySQL init script for spring-ai-alibaba-rag
-- This file is mounted into the MySQL container at /docker-entrypoint-initdb.d/
-- Tables will be auto-created by Spring Boot / JPA on first startup.
SELECT 'spring-ai-alibaba-rag MySQL init script loaded' AS status;
